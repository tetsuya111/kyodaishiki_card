# spec `rag-reference-control` の要件定義書を作成

- 日時: 2026-09-17 21:00
- 種別: docs（`.kiro/specs/`、Git 管理外）

## 何を行ったか

- `.kiro/specs/rag-reference-control/requirements.md` を新規作成した（ステータス: Draft）。
  - ストーリー1: `/rag last` の表示枚数の指定
  - ストーリー2: 対話で参照するカード枚数（`top_k`）をシェル内コマンドで表示・変更
  - ストーリー3: 参照するカードの固定（追加・削除・一覧・全解除）
  - ストーリー4: 固定カードを用いた対話（自動検索との併用、重複排除、文字数上限時の優先順位、`/rag last`・履歴での区別）
  - ストーリー5: 自然言語（ツール呼び出し）からの操作と `/help`
- `.kiro/specs/README.md` のスペック一覧に `rag-reference-control` の行を追加した。

## なぜ行ったか

- 利用者から次の方針で AI 対話シェル（`shells/llm.py`）を修正したいとの依頼があり、実装より先に spec を作成する運用（CLAUDE.md）に従った。
  - `/rag last` の枚数を指定できるようにする
  - 対話で参照するカードをコマンドから設定できるようにする
- 現状は参照枚数の変更に `config.json` の手編集が必要で、参照するカード自体は自動検索任せで指定できないため。

## 補足

- 依頼文の解釈（表示枚数か参照枚数か、カードそのものの指定か）は要件定義書の「制約・前提条件」に前提として明記し、「未解決事項」で確認事項とした。
- コードは変更していない。design.md / tasks.md は要件の合意後に作成する。
